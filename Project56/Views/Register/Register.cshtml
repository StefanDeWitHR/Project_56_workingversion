@model Project56.Models.Users


<div class="row">
    <div class="col-md-10 ">
        @using (Html.BeginForm("Register", "Register", FormMethod.Post ))
        {
            <legend>Registeren</legend>
                <div class="form-group">
                        <div class="col-sm-10">
                            @Html.TextBox("email", null, new { @class = "form-control" , placeholder="Email" })
                           
                        </div>
                        <div class="col-sm-10">
                            @Html.TextBox("password", null, new {  @class = "form-control", type="password"  , placeholder="Wachtwoord"}) 
                        </div>  

                <div class="form-group">
                        <div class="col-sm-4">
                            @Html.TextBox("fullname", null, new {  @class = "form-control"  , placeholder="Voornaam"})
                        </div>


                    <div class="col-sm-5">
                        @Html.TextBox("dt_birth", null, new {  @class = "form-control" , placeholder="Geboorte datum"  , type="date"})
                    </div>

                    <div class="col-sm-4">
                   
                            @{
                            List<SelectListItem> listItems= new List<SelectListItem>();
                            listItems.Add(new SelectListItem
                                    {
                                    Text = "Joel scholt",
                                    Value = "Joel scholt",
                                    Selected = true
                                    });
                            listItems.Add(new SelectListItem
                                    {
                                        Text = "Man",
                                        Value = "Man"
                                    });
                            listItems.Add(new SelectListItem
                                    {
                                        Text = "Vrouw",
                                        Value = "Vrouw"
                                    });
                            }

                                @Html.DropDownListFor(model => model.gender, listItems, "-- Selecteer geslacht --" , new {@class = "form-control"})
                    </div>
                </div>

                <legend>Adresgegevens </legend>

                    <div class="col-sm-10">
                        @Html.TextBox("a_adres", null, new { @class = "form-control" , placeholder="Adres" })
                    </div>
                    
                    <div class="col-sm-4">
                        @Html.TextBox("a_zipcode", null, new {  @class = "form-control" ,  placeholder="Postcode" })
                    </div>
                    <div class="col-sm-4">
                        @Html.TextBox("a_city", null, new {  @class = "form-control" , placeholder= "Stad"})
                    </div>
                    <legend>Bezorg gegevens </legend>
                
                    <div class="col-sm-10">
                        @Html.TextBox("d_adres", null, new { @class = "form-control"  , placeholder= "Bezorg adres" })
                    </div>
                    
                    <div class="col-sm-4">
                        @Html.TextBox("d_zipcode", null, new {  @class = "form-control"  , placeholder= "Postcode " })
                    </div>
                    <div class="col-sm-4">
                        @Html.TextBox("d_city", null, new {  @class = "form-control"  , placeholder="Stad" })
                    </div>
                </div>


                <input type="submit" value="Registeren"/>
                <br /> 
        }

            </fieldset>

